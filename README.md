# EventRegistrationPage_EBSB_IITH

An event registration page for EBSB events in IITH

# How to Run?

Run

```cmd
concurrently "cd server && npm run dev" "cd client && npm run dev"
```

to start the backend and react app